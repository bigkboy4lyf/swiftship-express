{
  "name": "swiftship-express",
  "version": "1.0.0",
  "description": "SwiftShip Express Shipping Website",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install",
    "build": "cd backend && npm install"
  }
}